name: CI

on:
  push:
    branches: [ main, dev, v2 ]
  pull_request:
    branches: [ main, dev, v2 ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
    - uses: actions/checkout@v5

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        version: "latest"

    - name: Install dependencies (all groups)
      run: uv sync --all-groups

    - name: Install XeLaTeX
      run: |
        sudo apt-get update -q
        sudo apt-get install -y texlive-xetex texlive-latex-extra texlive-fonts-recommended

    - name: Run tests with pytest
      run: uv run pytest --cov=docstream --cov-report=xml

    - name: Run ruff linting
      run: uv run ruff check .

    - name: Run ruff formatting check
      run: uv run ruff format --check .

    - name: Run mypy type checking
      run: uv run mypy docstream --ignore-missing-imports --no-strict-optional
      continue-on-error: true

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
